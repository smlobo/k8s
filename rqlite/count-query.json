[
    "select 'domain' as table_name, count(*) as row_count from domain",
    "select 'amelia' as table_name, count(*) as row_count from amelia",
    "select 'ryan' as table_name, count(*) as row_count from ryan",
    "select 'sheldon' as table_name, count(*) as row_count from sheldon",
    "select 'bliu' as table_name, count(*) as row_count from bliu",
    "select 'wasm' as table_name, count(*) as row_count from wasm",
    "select 'test-vue' as table_name, count(*) as row_count from \"test-vue\"",
    "select 'hikes' as table_name, count(*) as row_count from hikes"
]
